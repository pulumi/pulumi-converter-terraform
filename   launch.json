{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run dynamic_tested_for_each",
            "program": "${workspaceFolder}/path/to/test/file.js",
            "args": ["--test", "TestTranslate/dynamic_tested_for_each"],
            "env": {
                "NODE_ENV": "test"
            }
        }
    ]
}